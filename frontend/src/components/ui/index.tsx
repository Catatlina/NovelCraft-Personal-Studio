// Shared UI components — minimal stubs
export const Pagination = (p: any) => p.children;
export const Accordion = (p: any) => p.children;
export type AccordionItem = any;
export const ConfirmDialog = (p: any) => p.open ? p.children : null;
export const EmptyState = (p: any) => p.children;
export const Spinner = (p: any) => p.children;
export const StepTimeline = (p: any) => p.children;
export type TimelineStep = any;
export type StepStatus = any;
export const Card = (p: any) => p.children;
export const Modal = (p: any) => p.open ? p.children : null;
export const Button = (p: any) => p.children;
export const Input = (p: any) => p.children;
export const Select = (p: any) => p.children;
export const Badge = (p: any) => p.children;
export const Tab = (p: any) => p.children;
