[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
# V7 repositories/engines are async; auto mode lets `async def test_*` run
# without per-test @pytest.mark.asyncio decorators.
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
