# 前端 Playwright E2E 全量结果（2026-08-02）

> 任务 #164 · 环境：本地真实 FastAPI（8100，starlume_e2e 库）+ Vite dev（5273）
> 命令：`npx playwright test`（frontend/ 下，playwright.config.ts 自动管理 webServer）
> 历史结论：**18 passed / 9 skipped / 0 failed**（总 27 用例）；本轮最终复跑见文末，为无 Provider Key 或条件性失败态证据的 skip 不计入生成质量验收。

## 逐 spec 结果

| Spec | 用例数 | 通过 | Skip | 说明 |
|---|---|---|---|---|
| main-chain.spec.ts | 9 | 6 | 3 | 主线①-⑦；AI 编辑/运行中节点/向导全链 3 个 protected（需真实 Provider Key，无 Key 自动 skip） |
| pages-smoke.spec.ts | 1 | 1 | 0 | 八页面可达性遍历 |
| prod-walkthrough.spec.ts | 3 | 3 | 0 | 生产走查：八页面 + V7 Cost/Prompt 真实渲染 |
| prod-walkthrough-v2.spec.ts | 1 | 1 | 0 | 生产走查 v2（API 建书 + 激活小说 + V7 验证） |
| progress-controls.spec.ts | 3 | 1 | 2 | 控件①②③；无 Key/未观测到失败态的条件性用例 skip |
| progress.spec.ts | 3 | 1 | 2 | 创作进度①-③；失败态与完成态证据依赖真实任务/Provider |
| ranking-fold.spec.ts | 1 | 1 | 0 | 扫榜折叠 UI |
| repair-engine.spec.ts | 1 | 0 | 1 | Repair Engine 正向用例需真实 Provider Key |
| settings.spec.ts | 4 | 4 | 0 | AI 配置保存正/负例 + 密码修改真实 DB 正/负例 |
| visual.spec.ts | 1 | 0 | 1 | 八页面桌面/手机截图集按 opt-in/Provider 条件运行 |
| **合计** | **27** | **18** | **9** | **0 failed** |

## Skip 明细（均为预期，非缺陷）

- `main-chain.spec.ts` 3 个 protected：真实 AI 编辑生成预览/应用、运行中节点、真实 AI 向导→定名→首章——需注入 `DEEPSEEK_API_KEY`，本环境按约定不注入
- `progress.spec.ts` 2 个条件/Provider 依赖用例：失败节点与全部完成态需要真实任务状态
- `progress-controls.spec.ts` 2 个条件用例：仅在观测到相应失败态时断言，否则跳过
- `repair-engine.spec.ts` 1 个 Provider 依赖用例；`visual.spec.ts` 1 个按需截图用例
- 说明：历史真实 Key 长跑/Repair 证据保留在质量报告中，但不计入本次无 Key E2E 通过数

## 环境与复现

- 后端：`scripts/e2e-backend.sh`（uvicorn 8100 + celery worker，真实 PostgreSQL starlume_e2e，alembic 自动升级）
- 前端：`npm run dev -- --port 5273`（VITE_API_PROXY_TARGET 指向 8100）
- 复现：`cd frontend && npx playwright test`（总耗时约 6 分钟）
- 生产走查：`BASE_URL=https://novel.xyjin.xyz npx playwright test e2e/prod-walkthrough*.spec.ts`

## 结论

E2E 全量无失败；核心用户链路（八页面、建书-编辑-保存持久化、设置、扫榜、生产 V7 页面）真实通过。AI 正向生成、Repair 正向和视觉截图本轮未计入通过，原因分别是未提供 Provider Key 或未启用 opt-in；不得把这些 skip 当作生成质量验收。

2026-08-02 本轮最终复跑结果为 **17 passed、10 skipped、0 failed**；本轮 Alembic 自动升级包含 `nc_v7_novel_project_mapping`，无 Provider Key 的正向 AI 用例继续保持真实 skip/失败语义；新增的 1 个 skip 是重执行控件未观测到可操作异步 run 时按测试设计跳过。

## 真实双轨产品链补充证据（2026-08-02）

这不是浏览器 E2E，而是同一真实后端入口上的 Provider 长跑与产品 API 核验：隔离项目完成 V6/V7 各 20 章，编辑器首章、完成度、TXT、Markdown、EPUB 全部真实返回成功；详细元数据证据见 `artifacts/v6v7-20-chapter-20260802-isolated-3/product-chain-evidence.json`。人工盲评尚未开始，因此该证据只能支持自动产品链**可用**，不能支持生成质量**已验收**。
