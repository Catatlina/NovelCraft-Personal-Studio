# 第二次独立审计整改报告（2026-07-12）

> 基线：`28-全仓库审计报告-20260712-第二次独立审计.md`。本报告只登记可由代码仓库验证的整改，不把商业化缺口计入本轮范围，也不把尚未提供的域名、真实平台账号或外部服务可用性声明为已验收。

## 结论

审计列出的 B-2、N-1～N-20 均已完成代码整改；B-1 已交付 TLS/HSTS/HTTP 强制跳转配置，但证书签发与公网握手验收仍需真实域名。Medium/WordPress 发布已接入队列、加密账号凭据和重试链；其他没有稳定公开 API 的平台统一降级标记为半自动，不再虚报自动发布。

## 逐项对照

| 编号 | 状态 | 整改证据 |
|---|---|---|
| B-1 | 配置完成/外部待验收 | `nginx/novelcraft.conf`：80→443、TLS 1.2/1.3、HSTS；真实证书需域名 |
| B-2 / N-10 | 完成 | 前端默认同源 `/api`，支持显式 `VITE_API_BASE`，不再硬编码 8000 |
| N-1 | 完成 | SSE 改读 `run_nodes`，终态携带真实 run status |
| N-2 | 完成 | 删除重复 `/model-routes`，只保留 `/admin/model-routes`；写操作维持管理员门禁 |
| N-3 | 完成 | 服务/Worker 明确只读环境变量；交互 BYOK 只存 sessionStorage；禁止把运行时密钥写入无消费方 DB 设置；README 与页面同步 |
| N-4 | 完成 | 去掉 latency `+60`；按 Provider 输入/输出分别计价，支持 `AI_PRICE_CNY_PER_MILLION` 覆盖 |
| N-5 | 完成 | 结构化与流式调用的异常均写 `ai_calls(status=failed,error=...)` |
| N-6 | 完成 | 发布状态、历史、数据采集、统计、ROI、选题建议均校验内容成员或按用户项目过滤 |
| N-7 | 完成 | 删除短 ID 随机替换；首次状态只能是 draft，不能凭空 published |
| N-8 | 完成 | 创建发布记录后立即投递 Celery；从 `platform_accounts` 解密凭据；Medium/WordPress 执行结果回写同一记录；重试不再传空凭据；占位平台降级为 semi |
| N-9 | 完成 | 7 层上下文逐层执行预算上限，总预算硬限制，高优先层先保留 |
| N-11 | 完成 | 伏笔仅在计划回收章已到时报警；告警使用真实换行 |
| N-12 | 完成 | metrics 默认关闭；启用必须配置 `METRICS_TOKEN`，接口要求 Bearer token |
| N-13 | 完成 | Gemini API Key 改放 `x-goog-api-key` Header，不进入 URL/代理日志 |
| N-14 | 完成 | Provider 测试与拆书强制 project_id+编辑权限；测试密钥只收 Header；删除明文 credentials 发布入口 |
| N-15 | 完成 | Prompt JSON 示例移除多余反斜杠 |
| N-16 | 完成 | 编辑器加载全部章节并提供章节选择器，不再固定首章 |
| N-17 | 完成 | 清除 `row_to_dict` 死代码；`new_id` 将旧 prefix 参数明确标注为兼容参数，返回值始终满足 UUID 列约束 |
| N-18 | 完成 | CI 增加 prompt golden-case 独立门禁 |
| N-19 | 完成 | CI 增加 `pip-audit` 与生产依赖 `npm audit --omit=dev --audit-level=high` |
| N-20 | 完成 | 前端 Docker 改多阶段构建，运行阶段由 nginx 提供静态文件并反代 API |

## 本地验证

- `python -m compileall -q backend/app`：通过
- `python -m pytest backend/tests -q`：**426 passed, 4 skipped**
- `npm --prefix frontend run build`：通过
- `FLOWER_USER=test FLOWER_PASSWORD=test docker compose config -q`：通过
- `pip-audit -r backend/requirements.txt`：**No known vulnerabilities found**（同时迁移 python-jose→PyJWT，并升级 FastAPI/Starlette/FastEmbed/Pillow/pytest）
- `npm --prefix frontend audit --omit=dev --audit-level=high`：**0 vulnerabilities**
- `git diff --check`：通过

## 尚需外部条件的验收

1. 把 `example.com` 与证书路径替换为真实域名后执行公网 TLS 握手、续签与 HSTS 验收。
2. 使用用户自己的 Medium/WordPress 测试账号做沙箱/草稿发布验收；未提供凭据时只能验证队列、失败回执与重试语义。
3. CI 中真实 AI protected 用例仍取决于仓库 Secret；没有 Secret 时保持明确 skip，不计作通过。
