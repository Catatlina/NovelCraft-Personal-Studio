# NovelCraft Personal Studio — 全套开发文档索引（V2.1 定案版）

> 版本：v2.1 ｜ 日期：2026-07-10
>
> 本套文档是《NovelCraft Personal Studio》从**规划到开发落地**的完整文档集。相较上一版（重制版），本版完成两件事：
> ① **关闭架构评审全部 5 项 ⚠️ 遗留项**（多 Agent / 自动发布 / Offline First / 风格仿写 / 工程基线），评审结论升级为 **✅ 全量通过**；
> ② **补齐开发级文档全套**（数据库 / API / Prompt / 设计系统 / 测试 / 部署 / 环境 / 编码 / 安全 / ADR），并**重新生成需求规格说明书（PRD）**。

---

## 一、文档清单与阅读顺序

### A. 规划层（决策与范围）
| 编号 | 文档 | 作用 | 本版变更 |
|---|---|---|---|
| 01 | 项目全局分析报告 | 产品定位、需求本质、风险与难点 | Offline First ⚠️→✅ |
| 02 | 架构评审报告 | 8 大平台能力基线设计 + 评审结论 | **5 项 ⚠️ 全部定案关闭（§13）** |
| 03 | 开发路线图 | M1~M5 里程碑 + 250+ 功能覆盖映射 | 保持 |
| 04 | MVP 方案 | M1 范围、bootstrap 工作流、验收门禁 | 保持 |

### B. 需求层
| 编号 | 文档 | 作用 | 本版变更 |
|---|---|---|---|
| 08 | **需求规格说明书 PRD** | 13 大类 250+ 功能 → 8 能力，用户故事、验收、追踪矩阵 | **重新生成（新增）** |
| 19 | **开源项目融合基线** | 6个指定GitHub项目的许可证、融合边界、模块映射和门禁 | **V2.2 新增，强制输入** |

### C. 实现层（开发直接依据）
| 编号 | 文档 | 作用 |
|---|---|---|
| 05 | 技术实施方案 | 前后端目录结构、数据库、AI 实施、部署 |
| 06 | 开发任务清单 | TASK-001~052 全量任务 + 依赖 + 验收要点 |
| 09 | 数据库设计文档 | 26 表完整 DDL + 数据字典 + 索引 + 迁移 + seed |
| 10 | API 接口规范 | 13 模块 ~80 端点 + SSE + RBAC + 错误码 |
| 11 | Prompt 工程规范 | 10 Agent 契约 + 7 层装配 + golden case + 防侵权 |
| 12 | 设计系统规范 | Design Tokens（明暗双套）+ 组件库 + UI 验收 |

### D. 质量与运维层
| 编号 | 文档 | 作用 |
|---|---|---|
| 13 | 测试计划与用例 | 测试策略 + C1~C8 用例 + 里程碑门禁 + 压测 |
| 17 | 安全设计文档 | STRIDE + 认证授权 + AI 安全 + 上线检查清单 |
| 14 | 部署与运维手册 | compose + Nginx + 备份恢复 + 告警 + 故障排查 |
| 15 | 开发环境搭建指南 | 前后端本地栈 + 命令速查 + 新人 checklist |
| 16 | 编码规范 | 前后端规范 + 三条铁律 + Git/PR/CI |
| 07 | 工程协作规范 | 分支/commit/review/UI 验收/文档纪律 |
| 18 | 架构决策记录 ADR | 14 条关键架构决策留痕 |

**推荐阅读路径**：
- 产品/需求视角：01 → 08(PRD) → 19(融合基线) → 03 → 04
- 架构视角：02 → 18(ADR) → 05 → 09
- 开发上手：15(环境) → 16(编码) → 09(DB) → 10(API) → 11(Prompt) → 12(设计) → 06(任务)
- 测试/上线：13(测试) → 17(安全) → 14(部署) → 07(协作)

---

## 二、本版关键修复（架构评审 5 项 ⚠️ → ✅）

| 遗留项 | 原状态 | 定案措施（详见 02 §13） | 新状态 |
|---|---|---|---|
| 多 Agent | ⚠️ | AgentNode 契约固化 + CI 禁互调守卫 + Producer 只产 Plan + golden case | ✅ |
| 自动发布 | ⚠️ | 三模式定案 + 国内全自动默认关闭/二次确认/首篇 human/失败降级 | ✅ |
| Offline First | ⚠️ | L1 只读 / L2 乐观锁+三方对比 / L3 幂等重放；不做 CRDT/自动合并 | ✅ |
| 风格仿写 | ⚠️ | 三道防侵权闸 + 相似度阈值 0.75 强制重写 / 0.6~0.75 人工确认 | ✅ |
| 工程基线 | 有条件 | V1 收口(106 测试全绿)设为 C1 迁移硬前置门禁 | ✅（门禁化） |

---

## 三、核心架构速览

```
React + Vite + TS（PWA · 设计系统 · Tiptap 三栏工作台）
        │  REST + SSE
FastAPI（api → service → repository · JWT + Owner/Editor/Viewer）
        │
├── PostgreSQL + pgvector   唯一事实源
├── Redis                   Celery broker · 限流 · 预算 · 缓存 · SSE
├── Celery + beat           五队列 + 定时自动化
├── AI Gateway（进程内）      4 家 Provider 路由/降级/预算/校验/追踪
└── Nginx + HTTPS · Docker Compose · 单 VPS
```

8 大平台能力：C1 内容 · C2 工作流 · C3 AI Gateway · C4 知识 · C5 版本 · C6 叙事一致性 · C7 发布 · C8 追踪。
交付节奏：M1 扫榜成书+热点生产+地基 → M2 长篇小说引擎 → M3 内容工作室 → M4 发布数据出海 → M5 协作离线多端（约 9~10 个月）。

---

_文档纪律：本套文档为 V2 唯一基线；补代码同 PR 补文档；文档 PR 与代码 PR 同评审；禁止只有标题的空壳文档。_
