# 测试体系文档 (Enterprise Edition)

## 测试策略

分层金字塔：
- **单元测试**：pytest + pytest-asyncio，覆盖率 ≥ 80%
- **集成测试**：API + DB + Redis
- **E2E**：关键用户旅程
- **Golden Case**：每 Agent ≥ 1 条，进 CI
- **性能测试**：30 万字连载压测

## Golden Case 回归

每个 AgentNode 有 golden case：
```python
def test_writer_golden():
    result = agent_registry["Writer"].execute(GOLDEN_INPUT)
    assert result.word_count >= 2000
    assert result.has_dialogue
    assert schema_valid(result)
```

## 验收门禁

### M1 MVP
- 15 分钟灵感→过审第一章
- 杀 worker 断点续跑
- Budget 熔断生效
- 版本恢复正确
- ai_calls 记录完整
- 单测 ≥ 135 全绿
- 备份恢复演练通过

### M2
- 30 万字压测通过
- 一致性错误率 < 5%
- 7 维均分 ≥ 80
- 4 模型路由降级演练通过

## CI 自动化

```yaml
jobs:
  lint:    # ruff + mypy + eslint
  test:    # pytest + golden cases
  build:   # docker build
  security: # bandit + npm audit
```

## 性能基线

| 指标 | 目标 |
|---|---|
| API p50 | < 200ms |
| API p99 | < 1000ms |
| SSE 延迟 | < 2s |
| 自动保存 | < 500ms |
| Bootstrap 全链 | < 5min |
