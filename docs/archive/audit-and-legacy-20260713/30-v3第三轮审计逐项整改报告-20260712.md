# v3 第三轮独立审计逐项整改报告（2026-07-12）

> 外部报告：`审计报告_NovelCraft_v3.html`，审计 HEAD `028ad247`。当前整改分支基于该 HEAD 继续包含 PR #5 的修复，因此必须区分“报告在旧 HEAD 发现、现已修复”和“本轮新增修复”，禁止重复制造改动或把外部条件写成已验收。

## Blocker / P0

| 项目 | 结论 | 当前证据 |
|---|---|---|
| B-SSRF | **本轮修复** | `X-Api-Base-Url` 强制 HTTPS/443/精确 host allowlist，禁止 IP、内网、凭据、query、fragment；自定义 URL 必须同时使用请求级 BYOK；网关发请求前再次校验；ContextVar 请求后复位；10 条安全回归测试 |
| B-TLS | **PR #5 已修复** | nginx 80 仅跳转 443，TLS 1.2/1.3 + HSTS；真实证书握手仍需域名 |
| B-PUB / P0-PUBACC | **PR #5 已修复** | 发布记录投递 Celery，解密 `platform_accounts` 凭据，Medium/WordPress 回执更新同一记录，重试重新取凭据；无真实 API 的平台诚实标 semi |
| B-LONG | **证据边界补齐** | 已有真实 Provider 100 章 T5；本轮新增 1000 章/1,028,893 字符真实 PostgreSQL 存储与上下文预算探针。该探针明确 `ai_quality_claim=false`，不虚报百万字 AI 文学质量 |
| P0-SSE | **PR #5 已修复** | 改查 `run_nodes`；E2E/CI 通过 |
| P0-SET | **PR #5 已修复口径** | 交互 BYOK=sessionStorage+请求 Header；Worker/Beat=环境变量；禁止把运行时密钥写入无消费方 DB；页面/README 已同步，不再宣称 DB 即时生效 |
| P0-DOWN | **报告结论已过期** | 本轮两次在一次性 PostgreSQL 数据库执行 `upgrade head → downgrade base` 均成功；CI 新增完整往返门禁 |

## P1

| 项目 | 结论 | 当前证据 |
|---|---|---|
| model-routes 越权 | PR #5 已修复 | 删除弱权限重复路由，只保留 admin 写接口 |
| 发布历史泄露/指标伪造 | PR #5 已修复 | 内容成员校验 + 用户项目过滤 |
| 成本账本失真 | PR #5 已修复 | Provider 分价、真实 latency、失败/流式失败均入账 |
| assembler 预算 | PR #5 已修复 | 逐层预算、总预算、高优先级先保留；百万字探针组装上下文 2454 字符 |
| review 静默吞错 | **本轮修复** | OOC/一致性/节奏新增稳定 task_type、模型路由种子和严格输出契约；成功结果或失败 status/error 均写章节 meta；AI 失败同时进入 `ai_calls`；向量重建失败触发告警 |
| six_dim 占位 | **本轮修复** | 读取 `contents/entity_states/timeline_events/knowledge_items`，输出人物、地点、时间线、物品、设定、关系六类真实结构检查，不再返回全 pending |
| Agent 注册表空壳 | **本轮修复** | 8 个 Agent 映射到真实 Prompt/Gateway，新增带项目 RBAC、限流、幂等 mutation id 的 `/agents/{id}/execute` |
| RAG 默认哈希 | 报告结论已过期 | `fastembed` 已作为运行依赖；auto 默认 remote→local BGE→hash，`.env.example` 明示 local 模型；hash 只在后端失败时诚实降级并记录 provenance |
| 同步 urlopen 阻塞 event loop | **不构成报告所述运行时 bug** | AI 普通接口为同步 `def`（FastAPI 在线程池执行），Celery 任务为同步 worker，SSE 使用同步生成器（Starlette 在线程池迭代）；后续可独立迁移 async client，但不是当前 event-loop 直阻塞 |
| 真实 Provider 默认 CI | 条件性验收 | Secret 缺失时必须 skip，不能把无密钥调用伪装成功；有 Secret 时 protected T3/E2E 自动运行 |

## P2 / 生产配置

| 项目 | 结论 | 当前证据 |
|---|---|---|
| Tiptap 首屏体积 | **本轮修复** | Editor 改 `React.lazy`；产物新增独立 Editor chunk，Tiptap 仅进入编辑器时加载 |
| 性能索引静默吞错 | **本轮修复** | 修正 4 个旧错误列名，删除 `except: pass`；全量迁移往返实测成功 |
| 全局无限流 | 报告误判 | SlowAPI 已安装全局 `120/minute`，成本敏感接口另有 `20/minute` |
| Compose restart/端口/Beat | **本轮修复** | 长驻服务 `restart: unless-stopped`；PG/Redis/API 仅绑定 127.0.0.1；生产密码/JWT 必填；新增独立 Celery Beat 服务 |
| 弱密码 | **本轮修复** | 注册最小密码从 6 提升至 8，前后端一致 |
| 日志表无限增长 | **本轮修复** | Beat 每日清理过期 `ai_calls/operation_logs/audit_logs`，保留期由环境变量配置且最低 30 天 |
| Prompt 版本/Golden 声明 | **本轮修复真实性口径** | 修正后的模板发布为 1.0.1，不覆盖历史版本；无 Provider 的 CI 只验证 golden case 定义完整性并明确 `provider_outputs_verified=0`，不再把静态规则检查冒充模型质量通过 |
| main.py 高耦合 | 架构债，不是功能 bug | 本轮不做高风险大搬迁；已有新增功能继续放独立 core/service 模块 |
| Blue/Green、真实 TLS 证书、监控送达 | 外部部署条件 | 仓库已有 TLS 模板、Sentry/Prometheus/Telegram；域名、DSN、Bot、基础设施未提供时不能声称实测完成 |

## 本轮实测

- 后端：`437 passed, 4 skipped`
- 前端：TypeScript + Vite build 通过；Editor 已懒加载
- Alembic：两次独立临时库 `upgrade head → downgrade base` 通过
- 长篇规模探针：1000 章、1,028,893 字符；上下文 2454 字符；结果见 `artifacts/t5-long-run/million-character-scale-probe-20260712.json`
- Compose：注入强制环境变量后 `docker compose config -q` 通过

## 仍需用户提供的真实验收条件

1. 域名与证书，用于公网 TLS/续签/HSTS 验收。
2. Medium/WordPress 测试账号，用于真实草稿发布验收。
3. 仓库 AI Secret，用于每次 CI 强制跑 protected Provider 用例。
4. 若要求“百万字 AI 文学质量”而不仅是系统规模，需要批准相应模型费用与长时间运行窗口；当前不作虚假完成声明。
